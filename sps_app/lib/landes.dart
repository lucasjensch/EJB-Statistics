import 'package:flutter/material.dart';

class LandesPage extends StatelessWidget {
  const LandesPage({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(),
    );
  }
}
